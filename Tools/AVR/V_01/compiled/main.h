#include <Arduino.h>
#include "Analog.h"
#include "constants.h"
#include "boardSpecificsFunctions.h"


unsigned char pinLed	= 13;

void setup() {
	Serial.begin(9600);
	Serial.println("start");
	
	PINB |= B00100000;
	
	unsigned long t1 = micros();
	pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
pinMode(pinLed, OUTPUT);
	unsigned long t2 = micros();
	Serial.println(t2 - t1, DEC);
	
}

void loop() {
		// blink_very_slow
	/*
	pinMode(pinLed, OUTPUT);
	digitalWrite(pinLed, LOW);
	delay(500);
	digitalWrite(pinLed, HIGH);
	delay(500);
	
		// blink_slow
	*pinToDigitalPortMode(pinLed) |= pinToDigitalPinMask(pinLed);
	*pinToDigitalPortOut(pinLed) &= ~pinToDigitalPinMask(pinLed);
	delay(500);
	*pinToDigitalPortOut(pinLed) |= pinToDigitalPinMask(pinLed);
	delay(500);

	((bool) (*pinToDigitalPortIn(pinLed) & pinToDigitalPinMask(pinLed)))
	
		// blink_optimized
	PINB |= B00100000;
	PORTB &= B11011111;
	delay(500);
	PORTB |= B00100000;
	delay(500);*/
}